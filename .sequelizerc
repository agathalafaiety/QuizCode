const { path } = require('express/lib/application')
const patch = require ('patch')

module.exports = {
    config:patch.resolve('./database/config', 'config.js'),
    'models-patch':path.resolve('./database/models'),
    'seeders-patch':path.resolve('./database/seeders'),
    'migrations-patch':path.resoolve('./database/migrations'),
}
